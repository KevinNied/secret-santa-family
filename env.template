# ==================================================
# SECRET SANTA FAMILY - Environment Variables
# ==================================================
# 
# INSTRUCTIONS:
# 1. Copy this file and rename it to: .env.local
# 2. Fill in your actual values below
# 3. Never commit .env.local to git (it's already in .gitignore)
#
# ==================================================

# --------------------------------------------------
# DATABASE (PostgreSQL via Supabase)
# --------------------------------------------------
# Get from: Supabase Dashboard → Settings → Database → Connection string
# 
# Supabase uses PostgreSQL (Postgres) as the database
# We're using Prisma ORM to interact with it

# === Connection Pooling URL (RECOMMENDED for app queries) ===
# Use this for: Application queries, Server Actions
# Format: postgresql://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-us-west-1.pooler.supabase.com:6543/postgres
# 
# Note: Port is usually 6543 for pooling (not 5432)
DATABASE_URL="postgresql://postgres.xxxxx:password@aws-0-us-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

# === Direct Connection URL (for migrations) ===
# Use this for: Prisma migrations (prisma migrate dev)
# Format: postgresql://postgres:[PASSWORD]@db.xxxxx.supabase.co:5432/postgres
# 
# Note: Port is 5432 for direct connection
DIRECT_URL="postgresql://postgres:password@db.xxxxx.supabase.co:5432/postgres"


# --------------------------------------------------
# GMAIL SMTP (Email Service)
# --------------------------------------------------
# Configuración para usar Gmail SMTP para enviar emails
#
# IMPORTANTE: Para usar Gmail SMTP necesitas:
# 1. Activar verificación en dos pasos en tu cuenta de Gmail
# 2. Generar una "Contraseña de aplicación" (App Password):
#    - Ve a: https://myaccount.google.com/apppasswords
#    - Selecciona "Correo" y "Otro (nombre personalizado)"
#    - Ingresa "Secret Santa Family" como nombre
#    - Copia la contraseña generada (16 caracteres sin espacios)
#
# Tu email de Gmail (ejemplo: tuemail@gmail.com)
GMAIL_USER="tuemail@gmail.com"

# Contraseña de aplicación generada (16 caracteres, sin espacios)
# Ejemplo: abcd efgh ijkl mnop (escríbela sin espacios: abcdefghijklmnop)
GMAIL_APP_PASSWORD="tu_app_password_aqui"


# --------------------------------------------------
# APPLICATION SETTINGS
# --------------------------------------------------
# Base URL of your application
# Development: http://localhost:3000
# Production: https://your-domain.com
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# App Name (used in emails and UI)
NEXT_PUBLIC_APP_NAME="Secret Santa Family"


# --------------------------------------------------
# OPTIONAL: Advanced Configuration
# --------------------------------------------------

# Node environment (usually set automatically)
# NODE_ENV="development"

# Prisma Logs (opcional)
# Activar logs de queries de Prisma (true/false)
# Por defecto está desactivado para reducir ruido en consola
# PRISMA_LOG_QUERIES=false

# Enable debug mode for emails (logs instead of sending)
# DEBUG_EMAIL="false"

# Maximum participants per group
# MAX_PARTICIPANTS="50"

